---
- name: Jinja2 Template Demo
  hosts: all
  become: true

  vars:
    username: shreej hursale
    env: production

  tasks:
    - name: Generate welcome file
      template:
        src: templates/welcome.j2
        dest: /tmp/welcome.txt